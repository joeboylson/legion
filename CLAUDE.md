# legion

## Pipelines

Pipelines are Markdown files at `.legion/pipelines/<name>.md` holding a
Mermaid flowchart. Follow `templates/pipelines/README.md` when writing or
changing one, including the example in `templates/pipelines/feature.md`.
Operators read the flowchart themselves; Legion never parses it.
